---
name: woahh-overnight-3goals
description: "Overnight autonomous session (2026-06-07): 3 industry-level goals on 3 branches — restaurant templates, website UI redesign, native app — with founder decisions and crash-recovery pointer"
metadata:
  type: project
---

**Overnight autonomous build launched 2026-06-07** (founder asleep ~10h, wants industry-level results by morning). Full live plan + status was `docs/OVERNIGHT_PLAN_2026-06-07.md` (deleted 2026-06-10 docs cleanup — run complete; in git history).

**Founder decisions (before sleep):**
- **Authority = branches + previews ONLY.** No merge to main/master, no deploys, no live DB migrations. Review in the morning.
- Save progress continuously (commit + push branches; update the plan doc). Self-critique hard (adversarial agents + my own Playwright verify).
- Match orchestration to task: dependent build → pipeline workflow; divergent design → independent parallel agents + judge panel; research → fan-out + single-writer execution.

**3 goals, one at a time, 3 separate branches:**
1. **Restaurant storefront templates** — branch `feat/storefront-platform` (worktree `repo-audit`). The Theme-Shell framework (see [[woahh-storefront-platform]]). **✅ DONE (pending founder review) 2026-06-07.** 8 templates: 4 keepers (Aurora/Atelier/Noir/Harvest, customizable font/vibe) + 4 NEW bespoke completely-different layouts (**Kerb** food-truck, **Daily** cafe, **Maison** fine-dining, **Rush** QSR). Each functional across home+menu+checkout, desktop+mobile (headless-chromium verified). Commits `4f36ed8`→`68529ba`→`28afb96`→`44edaa6` (all pushed). Self-critique fixed: ThemeShell chrome theming, off-screen page-checkout (→full-screen takeover), preview-nav overlap, food heroes. **Preview-only** — NOT yet wired into the live `Shop.tsx`/picker (next integration after founder picks). Review: `/storefront-preview` (Cloudflare branch preview).
2. **Website UI upgrade** — branch `feat/marketing-home-redesign` off main (worktree `repo-home`). **✅ v2 ELEVATED 2026-06-08 (pending founder pick).** Founder feedback on v1: "looks good but elevate more — state-of-the-art / high IT industry grade, refine + build more." Now **6 directions** at top-tier-tech bar, each across **home + /eat marketplace + merchant dashboard**, non-destructive preview at `/home-preview`: **Momentum** (bold SaaS), **Warmth** (editorial/charity), **Clarity** (product-led, dark-native) — all REFINED with motion toolkits (IntersectionObserver staggered reveals, rAF count-ups, marquees, aurora/grain, 3D-tilt floating product mockups), bento, glass/depth, fluid clamp() type; + 3 NEW: **Nebula** (aurora gradient-mesh + glassmorphism), **Carbon** (developer-grade dark/mono/terminal — ⌘ command bar, `$ overview` console), **Lumen** (oversized-type bento, Awwwards energy). All reduced-motion + AA gated. Commits v1 `408b904`→`21c9f01`, review-fixes, v2 `3223c7f` + carbon count-up fix `5d56a81` (all pushed). Screenshot-verified all 18 screens; tsc clean, build green. Gotcha: local `vite preview` can serve stale dist (rebuild before serving); Cloudflare builds fresh.
3. **Native merchant app** — branch `feat/native-app-platform` off main (worktree `repo-native`). **✅ DONE 2026-06-07.** Decision-ready plan `docs/architecture/NATIVE_APP_PLATFORM.md` (Capacitor 8, phased PWA→single-app→white-label; Apple 4.2.6/4.3 trap + compliant Model A/Model C; IAP physical-goods exemption→keep Stripe; Capgo OTA) + committed Capacitor scaffold: `capacitor.config.ts`, `android/` project, `src/lib/native.ts` (isNativePlatform/nativeRedirectBase/forcedSlug), 10 auth/Stripe redirect sites migrated, SW gated web-only, forcedSlug seam, `scripts/build-merchant-app.mjs`. Verified tsc clean + build green + cap sync ok. Commits `eb61f2f`→`89f5ee0` (pushed). iOS needs a Mac; human store steps documented.

**ALL 3 GOALS COMPLETE + STAGED (branches+previews only, nothing merged/deployed). Morning-review guide was in the now-deleted `docs/OVERNIGHT_PLAN_2026-06-07.md` (run complete; in git history).**

**2026-06-08 — MERCHANT-OPERATIONAL HARD E2E (headless chromium vs LIVE test-bistro, on `feat/storefront-platform`):**
- **Storefront** ✅ renders. **Online ordering** ✅ (menu → add-to-cart → order lands in Kitchen "Awaiting approval"). **Cloud POS** ✅ = `WalkInOrderDialog` ("New walk-in order": menu grid, Counter/Dine-in/Takeaway, discount/tax/tip, Charge) + KDS kanban (New/Cooking/Ready/Completed) + a completed walk-in order present. Merchant login ✅ (`/business/auth?as=business`, Owner tab, email-or-username field is type=text).
- **2 real robustness bugs found + fixed (commit `da3140b`):** (1) `CardPayment` `loadStripe(undefined)` white-screened the WHOLE storefront when `VITE_STRIPE_PUBLISHABLE_KEY` missing → guarded (stripePromise=null, card disabled, order still places). (2) `storefrontConfigApi.getPublic` threw on the `get_public_storefront_config` 404 → React-Query retry-storm → storefront stuck on skeleton → now fail-safe (return null → default renders instantly). Both also matter for prod resilience.
- **Bespoke-template storefront E2E still BLOCKED on the 3 migrations** (`20260603010000`→`020000`→`20260607010000`) not yet applied to live + types regen. Online ordering + POS need NO new migrations (already live). C1 "no real cards until server-side total validation" hold still stands (orders create as `awaiting_confirmation`).

**2026-06-08 — 3 storefront migrations APPLIED to live (founder ran them); bespoke template render VERIFIED end-to-end** (published maison/kerb on test-bistro → `/shop/test-bistro` renders the bespoke storefront with live menu; add-to-cart + cart sheet work). Storefront fail-safe fixes shipped (`da3140b`: Stripe-key guard + getPublic 404). Live-wiring `7a573e4`.

**2026-06-08 — 6-HOUR AUTONOMOUS MISSION (founder away): get everything ready for the FIRST MERCHANT to onboard on `name.woahh.app` with CRM + cloud POS + online ordering (incl. ONLINE CARD PAYMENTS). "Everything functional is actually functional." Branches+preview only; merge to main ONLY after the restaurant approves.** Tasks: (1) guest checkout — anon-auth + email/T&C/marketing consent + account nudge (workflow `weex56az3`; needs migration `20260608010000_guest_checkout_consent` + Supabase anon-sign-ins toggle); (2) **C1 server-side order-total validation** so online payments are safe (the key blocker — migration on `create_order_with_inventory`); (3) hard-verify CRM + POS + ordering + payments; (4) website redesign preview running (6 directions, branch `feat/marketing-home-redesign`); (5) **consolidated FOUNDER HANDOFF doc `docs/FIRST_MERCHANT_LAUNCH.md`** = all migrations (in order) + Supabase/Stripe/Cloudflare actions + onboarding steps. Domain tiers captured in `docs/architecture/DOMAINS_ROADMAP.md` (3 tiers, build later). Keep committing+pushing; maximize workflows/agents; don't spiral/hallucinate.

**2026-06-08 — 6H MISSION DELIVERED (founder away).** All code-side built/reviewed/verified/pushed on `feat/storefront-platform` (NOT merged). Single source of truth: `docs/FIRST_MERCHANT_LAUNCH.md` (TL;DR ordered founder checklist) + `docs/sql/FOUNDER_RUN_THESE.sql` (migrations #2 guest-consent + #3 C1).
- **Guest checkout** ✅ built→adversarial-reviewed→bugs FIXED→re-verified→smoke-tested (`581bbad`→`bcf80fa`): email+T&C+email/SMS marketing+optional sign-in+account-upgrade nudge; anon-auth; claim-or-reject `upsert_my_consent` (no collision/orphan/dup).
- **C1 online-payment safety** ✅ (`a981b89`): server-side total floor; blocks 1¢-for-$80; adversarial-reviewed safe; migration #3 ready. **Card capture gated OFF** by `settings.payments.online_card_enabled` (founder flips per-merchant after C1 applied+tested).
- **Verified live (test-bistro, headless chromium):** CRM, cloud POS+KDS, online ordering, bespoke storefront render (maison/kerb), guest-checkout UI, + 8 day-1 dashboard pages.
- **Website redesign** 6 directions ready at `/home-preview` (branch `feat/marketing-home-redesign`) — founder picks.
- **FOUNDER ACTIONS (gated):** run FOUNDER_RUN_THESE.sql + regen types; enable Supabase anon sign-ins + Turnstile; test (ping me); flip card-capture after C1 test; pick a website direction; create first merchant + name.woahh.app. Domain tiers in `docs/architecture/DOMAINS_ROADMAP.md`. **Deferred v2:** full server-authoritative total (closes C1 fee-skip/promo-race).

**2026-06-09 — GUEST CHECKOUT + C1 VERIFIED LIVE end-to-end.** Founder ran migrations #2/#3 + enabled anon sign-ins. Live test then surfaced "Database error creating anonymous user" → root cause: the `auth.users` AFTER INSERT triggers (`handle_new_user_org` + `handle_new_customer_profile`) fire for anon guests + choke (no email/business). Fix = migration **#4 `20260609010000_guard_anon_user_triggers.sql`** (commit `7228919`): `IF COALESCE(NEW.is_anonymous,false) THEN RETURN NEW` guard on both (exact bodies + guard line; appended to `FOUNDER_RUN_THESE.sql`). After founder ran #4: **anon sign-in OK → consent #2 persisted (email+marketing_opt_in+tos_accepted_at) → guest order placed (b7cd95ec, awaiting_confirmation) → C1 #3 REJECTED a tampered $0.01 order ("below the authoritative minimum 350 cents").** All 4 migrations live + verified. **Remaining for go-live:** Stripe Connect + flip `settings.payments.online_card_enabled=true` (real cards); **captcha currently OFF** (toggled off to allow auto-test) — fine for pilot (Supabase built-in anon rate-limit), wire hCaptcha widget + re-enable before scale; anon-user cleanup cron before scale; regen types.ts (optional). NOTE for first-merchant onboarding: an anon guest who UPGRADES to a real account now skips `handle_new_customer_profile` (no growthhub_profile on upgrade-via-updateUser, which is an UPDATE not INSERT) — profile-on-upgrade is a known follow-up if cross-merchant /account identity is needed for upgraded guests.

**2026-06-09 — OPTION B DONE: bespoke/premium storefront takes ONLINE CARD PAYMENTS** (was: orders-only/pay-at-venue). Reused the default's exact Stripe flow (`CardPaymentDialog`+`stripe-payment-intent`+C1 server total+manual-capture-on-confirm; NO new payment path), same gate (`online_card_enabled` && server-side `charges_enabled` && !dine_in). Made the bespoke Payment step **context-aware** (was showing decorative INERT card fields in all modes → misleading/double-entry): preview/demo=decorative showcase, live+payOnline="Pay by card" panel→real dialog, live+pay-at-venue="No payment needed now" panel. `onlineCardEnabled` threaded onto StorefrontWorld (single source of truth, bridge reuses → UI+gate can't diverge). Adversarial-reviewed (correctness PASS/preview APPROVE), tsc clean, build green, 73/73 tests, smoke-verified live (pay-at-venue panel renders, no fake fields, no errors). Commits `8e4b7e2` (card wiring) + `b9f5271` (Payment-step UX). Files: PublishedStorefront.tsx, screens/Checkout.tsx, chrome/contracts.ts (CardPayment.tsx + stripe-payment-intent UNCHANGED). **Live card E2E (real Stripe test card) = Phase 9 with merchant's connected account.** Full onboarding runbook: `docs/launch/MERCHANT_ONBOARDING_RUNBOOK.md` (10 phases, who-does-what).

**2026-06-09 night — OVERNIGHT SQUARE RUN (founder asleep, authorized "run it all").** Full plan: `docs/OVERNIGHT_SQUARE_PLAN.md`. Authority: build + commit LOCAL ONLY on `feat/storefront-platform` (NO app pushes — Cloudflare); docs push to hub-files (no CF); deploy edge fns to Supabase only; sandbox-only Square testing (founder live acct NOT needed). Founder inputs: Square Sandbox App ID `sandbox-sq0idb-MNrX3w-C63OrW82EZjLr3Q`, `SQUARE_ACCESS_TOKEN` sandbox secret set in Supabase. **WAITING on: a Supabase access token (sbp_) to deploy+sandbox-test autonomously — else build+verify only.** Priorities: (1) Square ONLINE payments fully functional (workflow `wzi6jmhua` building square-payment/square-webhook/order-respond-capture/provider-flag/Web-Payments-SDK/CSP, C1-gated); (2) multi-location + one-and-done org-level OAuth connect (staff/tablet-independent) + GMV view + refunds (RefundPayment); (3) templates-functional + scalability audit; (4) repo cleanup + fix CLAUDE.md. Invariants: payment = adapter (`payment_provider` per org default stripe, Stripe untouched); additive+idempotent schema only (no data-loss); C1 server total is the only charge amount. Already committed LOCAL (unpushed) on the branch: demos (`a9092f4`) + Taco Cantina template + green/red accent swap (`909d918`). Bespoke storefront online card capture done earlier (`b9f5271`). Square plan doc: `docs/architecture/SQUARE_POS_INTEGRATION.md`. Local-only commits → ONE final app push when founder approves.

**2026-06-10 — OVERNIGHT SQUARE RUN ~COMPLETE.** Morning report: `docs/MORNING_REPORT_2026-06-10.md`; one-file migration bundle: `docs/FOUNDER_RUN_NEXT.sql` (10 unrun migs in order). Delivered (all committed LOCAL on `feat/storefront-platform`, 6 commits ahead, **UNPUSHED**, nothing deployed/merged): Square online payments (a9aef21) + scale features (65e42b4: one-and-done org OAuth connect `square_connections`/per-org tokens [fixes BLK-2], multi-location, GMV `get_gmv_analytics`, refunds `refund-order`+`payment_refunds`+`REFUND_POLICY.md`); deep audit (`docs/security/AUDIT_FINDINGS_2026-06-09.md`) → fix-1 (46df6cc: BLK-3/4 money+sold-out, H-4/8/9/10/11/12) + fix-2 (c01bcf6: BLK-1 claim-CAS double-charge race, H-1/3/5/6/7). Rigorous Playwright testing (48 storefront combos + dashboard new-UI graceful-degrade, no product bugs). Repo cleaned (8 superseded docs → docs/archive/, README index, .DS_Store gone), CLAUDE.md updated to current reality (f27ce04). 10 NEW migrations to run (20260609020000/030000/040000/050000/060000 + 20260610010000/020000/030000/040000/050000) + regen types. Final integration review DONE (`wh7w5hjcp`) → caught 3 more deploy blockers (INT-B1 refund-state clobber re-marking refunded→paid/re-inflating GMV, INT-H1 anon-tracker leaks square_location_id via migration order, INT-H2 refund lock-order deadlock) → ALL FIXED + reviewed-correct + tsc/build green, committed LOCAL `2d99b4f` (+ new 11th migration `20260610060000_remask_get_order_by_id`, MUST run last). `FOUNDER_RUN_NEXT.sql` now bundles 11 migrations. **7 local commits ahead, unpushed.** **Autonomous deep work COMPLETE** — payment surface reviewed 4× + hardened 2×; remainder is FOUNDER-GATED (deploy + live sandbox test). **FOUNDER ACTIONS (morning report): run FOUNDER_RUN_NEXT.sql → Square OAuth app + secrets → deploy 7 edge fns → ping me for the live sandbox test (4111… card, connect→authorize→capture→refund) → then the ONE final app push.** Still NO Supabase token (couldn't deploy/test autonomously). Deferred-for-decision: H-2 (inventory release on payment failure), Square AU/AFSL, in-person Terminal.

Crash-safe: `/workspaces` persists across container crashes (prior crash kept committed + untracked work); app-repo branch pushes are extra safety; docs-repo push may 401 (needs devsup76 PAT) — commit locally regardless.
