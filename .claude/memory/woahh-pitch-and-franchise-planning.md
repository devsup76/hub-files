---
name: woahh-pitch-and-franchise-planning
description: "2026-06-02 planning session — pitch/positioning docs + franchise architecture + new todos 6.1-6.5, all pushed to devsup76/hub-files"
metadata: 
  node_type: memory
  type: project
  originSessionId: f10c18b2-0a3b-4f7d-8fc2-09914b89bde1
---

2026-06-02 planning session (docs-only; no app code changed). All committed + pushed to the planning repo `devsup76/hub-files` master (see [[woahh-planning-repo-remote]]).

**Pitch & positioning** (`docs/pitch/`): `RESTAURANT_PITCH.md` (merchant sign-up pitch — numbers/$30-order math, full daily loop online→walk-in terminal→customer DB→offers, hardware tablet+kitchen-screen+Stripe-Terminal story), `VC_PITCH_DECK.md` (15-slide deck + speaker notes), `POSITIONING_BRIEF.md` (feature inventory + differentiators + roadmap). Built via a multi-agent Workflow.

**Franchise / multi-location** — designed + approved, **NOT built** (build later, post-onboarding). Full plan in `docs/architecture/FRANCHISE_ARCHITECTURE.md`, todo `6.5`. Strictly additive; franchise layer above organizations; cross-org access via `franchise_members` overlay (owner_id/staff UNIQUE not relaxed); nullable `organizations.franchise_id`; reuse `growthhub_profiles`/`merchant_connections`. Decisions: read-only oversight first, loyalty configurable per brand, campaigns franchise-wide+per-location, manager scope configurable / service per-store. Only non-additive bit: a `kind='franchise'` skip in `handle_new_user_org()`.

**New todos 6.1-6.5** in `docs/WOAHH_FIXES_TODO.md`: 6.1 disable delivery behind a flag; 6.2 founding launch promo (free sub + zero commission, first N); 6.3 UI uplift; 6.4 restrict CRM to owner+manager (drop `"Staff view customers"` RLS policy — client already gates it); 6.5 franchise.

**Open decisions to resolve before acting** (flagged in the docs, not yet decided): founding-offer count + 1yr-vs-lifetime free sub + commission window (6.2); hardware lease-vs-included (BUSINESS_STRATEGY §12); reconcile charity-% (code 0.1% GMV floor + 50/50 split vs other docs) and Growth price ($150 vs $199) before the VC deck ships.

**Marketing landing refresh (CODE, on a branch — 2026-06-02):** in the APP repo `repo/` (`devsup76/business-growth-hub`), branch **`feat/marketing-landing-refresh`** @ `ed6be97`, PUSHED (Cloudflare preview building), **NOT merged** — awaiting founder review. Rewrote `src/pages/Storefront.tsx`: founding-merchant announcement bar + offer (0% commission + free subscription) in hero/pricing/CTA; new AI section (menu import/campaign/decline — live); expanded features to the real shipped set; moved delivery + installable app + Stripe-Terminal + custom-domain to an honest "On the way" row (REMOVED prior overclaims "White-Label Delivery" + "Native iOS & Android Apps" which were advertised as live); new "Why woahh wins" competitor comparison table; new FAQ (native `<details>`); homepage `updateMeta()`. Discoverability for AI/search: `index.html` rich meta + canonical + 3 JSON-LD blocks (Organization, SoftwareApplication w/ pricing Offers, FAQPage); new `public/llms.txt`; `sitemap.xml` +/demo. Build green; Playwright-verified desktop+mobile. **Loose ends:** `og-image.png` still missing → temporarily pointed og:image to existing `/icon-512.png` (founder should add a proper 1200×630 card); founding-offer duration still OPEN (6.2).

**Same branch — later commits (also pushed, @ `375e646`, still NOT merged):** (1) Email-live / SMS-shipping-shortly status across landing; AI menu import reframed as a conversational chatbot; restored Live-demo nav link. (2) **App-wide green/gold theme + dark mode** — `src/index.css` remaps shadcn semantic tokens (were ink-black + indigo) to brand forest-green primary + gold accent/ring/highlight for BOTH light and a new dark palette (deep forest + gold primary); wired `next-themes` ThemeProvider (was installed but unused) + new `src/components/ThemeToggle.tsx` in the dashboard header. Marketing page keeps its fixed `brand-*` palette. (3) **Merchant branding now consistent on customer surfaces** — applied the canonical `useStorefrontSettings(org)` (colours+font) to RetailStorefront (+merchant logo in header), OrderStatus, ReservationBooking (previously only RestaurantStorefront branded); `marketplaceApi.getById` now returns `settings`. Account + MarketplaceProfile intentionally stay global Woahh brand. Playwright-verified light+dark. (4) Contrast pass: `--primary-foreground`=gold (green buttons→gold text), `--accent` remapped pale-gold→visible green (fixed ~50 `text-accent` spots), gold reserved for fills/rings (no gold text on white); comparison table expanded to 16 rows incl. online/QR ordering + "Soon" pills for delivery/install-app/tap-to-pay; "Why Woahh" casing.

**MERGED TO MAIN 2026-06-02** — `git merge --no-ff` → main `801abf3` (was `a88a089`), pushed to `devsup76/business-growth-hub`; Cloudflare rebuilds woahh.app prod from main. Build green. Branch work fully shipped. Still open (post-merge polish, non-blocking): founding-offer **duration** wording (1yr vs lifetime, 6.2) and a proper **1200×630 og-image** (currently og:image→/icon-512.png).

**Why:** founder wanted teammate-visible pitch material + an industry-level franchise plan that can be built later without losing data + the public landing to out-position competitors for humans AND AI comparison tools. **How to apply:** CLAUDE.md "Outstanding TODOs" references the docs; the landing branch awaits founder sign-off on the Cloudflare preview before merge to main.
