---
name: woahh-ci-safety-spine
description: Building the GitHub CI + PR safety spine for the woahh app repo (autonomous-agent guardrail); branch ci/safety-spine, NOT merged
metadata:
  type: project
---

**Goal (2026-06-14, founder asked "set up GitHub CI + PR safety spine, industry-level/state-of-the-art, so functionality never breaks").** This is the "crawl phase" of [[woahh-agentic-dev-workflow]] — the missing safety spine before autonomous coding agents touch the live payments app. Done in **workflow + auto mode, ultracode**.

**Where:** app repo `devsup76/business-growth-hub`, worktree `/workspaces/GrowthHub/repo-ci`, branch **`ci/safety-spine`** off `origin/main` @ `869d676`. **Additive-only** (touches no existing src/ or migration). Branch→preview→founder-merge (NOT merged; I only push the branch).

**Measured baseline on main @ 869d676 (authoritative):** vitest **82 GREEN**; vite build GREEN (incl. `check:layers` prebuild); raw `tsc -b` **RED (6-7 stale types.ts errors)** → typecheck is a RATCHET; raw `eslint .` **RED (4 errors/30 warnings, incl. 2 real react-hooks/rules-of-hooks)** → eslint is a RATCHET (errors-only, warnings never block). Two lockfiles (bun.lock + package-lock.json); Node 20 (.nvmrc); pkg scripts build/test(vitest run)/lint(eslint .)/check:layers.

**Gate tiers:** BLOCKING (green-on-main): build-test, migration-guard (diff-scoped), no-test-deletion, secret-scan (gitleaks PR-diff only — history has an exposed ghp_ PAT so full scan would red every PR). RATCHET (fail only on NEW vs committed baseline): typecheck, eslint-errors. ADVISORY (continue-on-error, never block): codeql, rls-test (pgTAP cross-tenant), ci-integrity (anti-CI-gaming ::warning::).

**Process:** (1) design workflow `wnoldtqqn` (9 authors) → 18 files. (2) Assembled + generated REAL ratchet baselines + **proved every blocking gate green-on-main AND biting on injected regressions**. Caught+fixed 2 bugs (gitleaks .toml per-rule allowlist must be singular `[rules.allowlist]`; check-migrations env var BASE_SHA). Committed `3e70dff`. (3) Adversarial-verify workflow `w4ewfjsmz` (8 skeptic lenses, web-enabled) → **6 CRITICAL + ~15 HIGH** found, many empirically verified. Full findings: `/home/vscode/.claude/projects/-workspaces-GrowthHub/c4cc70f6-.../tool-results/bondfviri.txt`.

**CRITICAL findings being fixed (5-agent fix workflow, disjoint files):**
- secret-scan reds EVERY PR (gitleaks-action needs GITHUB_TOKEN, never mapped; +org-license landmine) → switch to gitleaks **CLI** direct, explicit base..head, no token/license.
- migration-guard FP on **30/129** real migrations (ALTER…DROP whole-file regex crosses `;`) → per-statement scan + strip `$$`/string-literals.
- migration-guard **RLS-leak FN**: DROP POLICY / USING(true) / DISABLE RLS on EXISTING tenant tables passes clean → add RLS-weakening detectors + base-tree tenant-table set + tautology variants (OR true, 1=1, ((true))).
- branch-protection gh-api script **422s** on user account (sends restrictions.teams) → account-type-aware, restrictions:null for User.
- edge functions (supabase/functions, where payments live) have **ZERO** CI analysis → add advisory deno check/lint.
- bun.lock/package-lock **drift** → delete bun.lock; dead `--depth=0` fetch step → remove.

**Supply-chain lens CLEAN:** all action tags exist (checkout@v4.2.2, setup-node@v4.1.0, gitleaks-action@v2.3.7, codeql-action@v3.27.5). SHA pins available for hardening. devsup76 = User account (no gitleaks license needed NOW; landmine if transferred to an org).

**Founder action items (operational, NOT code):** rotate the live ghp_ PAT in the git remote URLs (nullifies every gate until done); ~~APPLY branch protection~~ **DONE 2026-06-14 (applied live via API)**; decide on `supabase-deploy.yml` (auto-applies migrations on merge — CONTRADICTS "migrations never auto"; found untracked in a worktree).

**PR #3 MERGED to main 2026-06-14 (squash, admin override) → main @ `27aad38`; ci.yml now ON main → CI gate is repo-wide (every future PR to main runs the 6 blocking checks).** The spine is fully live: branch protection enforcing + workflows on main + proven green. `ci/safety-spine` remote branch DELETED post-merge (local worktree repo-ci still exists, on the now-orphaned local branch @ be753d1 — harmless). To gate a feature branch: rebase it on the new main so it carries ci.yml (else checks show "Expected" → admin override).

**BRANCH PROTECTION APPLIED LIVE 2026-06-14** to `main` via API (workflow `wg51atk9t` derived+verified the payload; 3 lenses+synthesis agreed). Settings: required checks = the 6 blocking job names (strict/up-to-date), `enforce_admins=false` (SOLO-FOUNDER bridge — founder can admin-override-merge his own work, agent bot stays blocked; flip to true when a 2nd reviewer exists), require_code_owner_reviews=1 + dismiss_stale + require_last_push_approval, no force-push/deletions, required_linear_history + conversation_resolution, `restrictions=null` (User repo — the only accepted value; `bypass_pull_request_allowances` is org-only and 422s → removed). **Founder merge note:** with enforce_admins=false the founder can merge ANY branch (incl. ones lacking ci.yml → checks show "Expected" → use the admin "merge without waiting" button); direct push to main still works for admin. Cleanest: merge PR #3 first so ci.yml is on main, then other branches run the 6 checks.

**Residual/documented limitations:** ratchet count-collision (fix-one-add-one same file+rule nets zero), agent-controlled escape hatches (`-- ci:allow-*`, `[ci allow-test-removal]`), SHA-pinning deferred (Dependabot configured), no npm-audit/dependency-review gate yet.

**STATUS 2026-06-14 — COMPLETE + VERIFIED, committed `be753d1` on `ci/safety-spine` (single commit, 23 files), but NOT pushed.** All 6 criticals fixed via the 5-agent fix workflow `wwvrenw5o` + re-verified locally: migration-guard rewrite (per-statement) — realistic additive migration (ADD COLUMN + DROP CONSTRAINT + DROP/CREATE POLICY + fn body w/ 'truncate' string) PASSES (FP fixed), while DROP TABLE / UPDATE-no-WHERE / DROP-POLICY+USING(true)-on-orders / DISABLE-RLS all BLOCK. secret-scan = gitleaks CLI (checksum 5bc41815…e3ba VERIFIED matches real 8.21.2 binary), PR-diff base..head, no token/license. vitest 92 green (incl. 10 ratchet self-tests), both ratchets green+bite, build green, gitleaks scan clean.
- **Recovered from a git mishap:** during a migration negative-test, `git add -A && git commit` swept all uncommitted fix-work into a scratch commit, then `git reset --hard HEAD~1` discarded it from the worktree. Recovered from dangling commit `240090e` via `git checkout 240090e -- .` (minus the swept-in supabase-deploy.yml + test migration). LESSON: commit fixes BEFORE doing scratch-branch destructive testing.
- **PUSHED + PR OPENED + CI GREEN 2026-06-14:** **PR #3 → https://github.com/devsup76/business-growth-hub/pull/3** (base main), head `bae32eb`. **All 6 BLOCKING gates pass on real CI** (build-test, typecheck-ratchet, lint-ratchet, migration-guard, no-test-deletion, secret-scan) + ci-integrity + Cloudflare Pages + **rls-isolation now green** (fixed: anon assertions use `throws_ok(42501)` — commit `bae32eb`; the cross-tenant isolation core `ok 1-11` already passed first run). **Only red = CodeQL (advisory, non-blocking): repo error "Code scanning is not enabled" (API 403)** → founder has paid GitHub + GHAS available (private repo, User acct), so KEEP codeql.yml; enable code scanning at **Settings → Advanced Security → Code Security → Set up → Default → Enable CodeQL** (the older "Code security and analysis" path was wrong). My token = repo+workflow scopes only (no security_events/admin) so I can't enable it via API. Considered swapping to Semgrep (free, no-GHAS SAST) but founder has GHAS → kept CodeQL; semgrep draft removed unpushed. secret-scan (gitleaks CLI) CONFIRMED working on a real runner. The default PAT lacked `workflow` scope (rejected); founder pasted a workflow-scoped `ghp_FYnF…` token used for a one-off push (NOT persisted in .git/config). **ROTATE that pasted token — it's in chat plaintext** (on top of the pre-existing exposed ghp_ in the remote URL).
- **Founder action list (in docs/ci/CI_SAFETY_SPINE.md + BRANCH_PROTECTION.md):** (1) push branch w/ workflow-scoped PAT; (2) rotate the live ghp_ PAT in the git remote URL; (3) apply branch protection via the account-type-aware `gh api` script (spine is bypassable until applied); (4) do NOT add the untracked `supabase-deploy.yml` (auto-applies migrations on merge — contradicts human-applied-migrations guardrail); (5) review/merge to main.
