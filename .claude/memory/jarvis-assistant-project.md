---
name: jarvis-assistant-project
description: "User's personal \"Jarvis\" (Iron Man-style voice AI + HUD) project — vision, hardware, and recommended architecture"
metadata: 
  node_type: memory
  type: project
  originSessionId: 213112d3-cbd5-4cb0-9221-3a0ba2396e4e
---

User (big Iron Man / Jarvis fan) wants to build a Tony-Stark-style **Jarvis**: high-tech HUD UI + always-on voice assistant + agentic "hands" (system/home control). Started 2026-06-12. This is a SEPARATE project from woahh.

**Environment / hardware (durable constraints):**
- Currently: Claude CLI inside a **Docker dev container** (headless — no mic/speakers/display/GPU; good as a sandbox + build env, bad as the voice/HUD host).
- Coming later: a **separate Mac** with **Claude running at root** (that Mac has **no sensitive info**, so broad agent permissions are acceptable). This Mac is the natural always-on Jarvis runtime host (owns mic/speakers/displays/Apple Silicon GPU/HomeKit).
- Displays: **laptop + 2 monitors (3 screens)** + a **Google TCL TV (C635 or C645, 4K 144Hz)** — ideal as a wall/ambient Iron-Man HUD canvas.

**Recommended architecture (my advice, pending his choice):** run the Jarvis RUNTIME on the **Mac** (voice I/O, wake word, TTS, HUD on TV/monitors, OS + smart-home control, Claude-at-root = "hands"); keep the **container** as the isolated SANDBOX for risky tool execution + the dev environment to scaffold the HUD web app + agent now. They talk over LAN (WebSocket/HTTP). "Both, with roles." Brain = **Claude Agent SDK** + MCP tools. Voice loop = **Pipecat** (VAD/barge-in) with **openWakeWord "hey jarvis"** + **Whisper** (local) or Deepgram STT + **ElevenLabs** British TTS (Piper local fallback). HUD = React/Three.js web app (reactive orb + clock/weather/calendar/system-stats/transcript) in Chrome kiosk/Electron, fed by WebSocket.

**Phased plan:** P1 push-to-talk→STT→Claude(tools)→TTS (voice MVP on Mac). P2 wake word + barge-in + web HUD. P3 real tools (calendar/email/Home Assistant/HomeKit/media/TV/system + proactive briefings + memory). P4 multi-screen command-center + camera/vision + custom voice.

**LOCKED topology (2026-06-12):** (1) **Home Mac** — always plugged in/charging, drives **2 monitors + the TCL TV** in one room = the **Jarvis CORE/brain + HUD host** (always-on resolves the reliability caveat). (2) **Roaming Mac** — moves with him (home/uni/anywhere) = mobile client; can also hold the woahh repo + work hands-on. (3) **Container ("here") = the SECRETS VAULT** — secrets live ONLY in the container and are ROTATED; it's also the secure worker. (4) **iPhone** — to be linked as a pocket client.

**Security model = "container as the single secrets broker."** woahh repo (code) may live on BOTH Macs for editing, but NO secrets on the Macs/phone — every credentialed action (git push, deploy, Supabase service-role, ideally even the Anthropic API call via a proxy) routes THROUGH the container, which holds + rotates all keys. All other devices (both Macs, iPhone) are credential-free clients → losing any device leaks nothing; one rotation point. Keep `.env` out of the repo on the Macs (gitignore + gitleaks); treat the container's repo copy as canonical (it's what pushes).

**Mesh VPN = Tailscale** (linchpin): home Mac exposes nothing public; roaming Mac + iPhone reach the core over the encrypted mesh as if on the same LAN. iPhone link = SECURE because it's a thin client holding no secrets: Tailscale-on-iOS → Siri Shortcut / PWA / AirPods → talk to the home-Mac Jarvis anywhere; privileged/destructive actions gated off the phone (voiceprint/confirm); instant revoke if lost.

**KEY FACT (2026-06-12):** the current container (woahh repo + secrets) runs on the **ROAMING Mac** — the travelling machine. That's the *worst* long-term home for the vault (it goes to uni / public Wi-Fi / theft risk). **DECISION:** when the home Mac is set up, **re-home the vault container onto the always-on home Mac** (Option A, OrbStack + FileVault) and demote the roaming Mac to a credential-free client that reaches the home vault over Tailscale. Interim hygiene: FileVault ON on the roaming Mac now; ROTATE all keys after the migration (retire any that travelled on the laptop); wipe the old container's secrets post-move. Offline-at-uni is handled by reaching home over the mesh (not by carrying secrets); true-offline local fallback is a later edge case.

**LOCKED specs + decisions (2026-06-12):**
- **Home Mac = MacBook Pro M1 Pro** (always-on, plugged, clamshell driving 2 monitors + TV). Confirmed it handles the full LOCAL voice stack + orchestrator + HUD + Claude Code worker with headroom (the heavy inference is cloud-side). Electricity not a concern.
- **Privacy-first (user: "not a fan of being listened to on the cloud 24/7"):** wake word (**openWakeWord "hey jarvis"**) + **STT (MLX/faster-whisper)** stay **fully on-device** — mic audio NEVER streamed to a cloud STT vendor; nothing leaves until "Jarvis" is spoken.
- **Voice/TTS = ElevenLabs Flash (~$30/mo) for the butler feel, with a one-toggle local Kokoro (MLX-Audio) "private/offline mode"** (Kokoro sounds good on M1 Pro). Cloud TTS only sends OUT spoken text, not mic audio — acceptable to user.
- **Brain:** heavy dev work → Claude Code on the user's **existing $200 Claude Max (20x)** subscription (CLI/`claude -p`, rides included programmatic credit pool + interactive lane); **snappy voice chat → thin Haiku API (~$3-5/mo)**.
- **Budget:** user OK with **$20–40/mo API on top of the $200 Max**. Recommended config = ElevenLabs $30 + Haiku ~$5 ≈ **$33-35/mo incremental** (drops to ~$5 if TTS goes local).
- **VPN:** user has **NordVPN (paid)**, considering **ProtonVPN**. Clarified mesh ≠ commercial VPN: **get Tailscale (free) for the Jarvis device-mesh** regardless; Nord/Proton is a SEPARATE browsing-privacy choice (Proton = stronger privacy pick). Heads-up: split-tunnel so Tailscale ≠ routed through Nord/Proton; Headscale = max-private self-host option.

**OVERNIGHT BUILD DONE (2026-06-12):** user said "build everything", confirmed (1) full overnight build, (2) voice = local Kokoro `bm_george` default + Polly "Brian" behind a flag. Built in **`/workspaces/GrowthHub/jarvis/`** — **git-EXCLUDED via `.git/info/exclude` (out of both repos, committed/pushed to nothing)**; ~48 files, 7MB. Contents: `SPEC.md` (full arch incl. §6.5 adaptive HUD), `RUNBOOK.md` (Mac setup), `MORNING_REPORT.md` (read first), `docs/RESEARCH_NOTES.md`, `.env.example`, `docker-compose.yml`. **HUD** (`hud/`, Vite+React, BUILDS GREEN) = reactive Canvas2D orb + adaptive scene system (ambient/lab/cast/canvas) — **4 scenes screenshotted in `assets/hud-preview/`**. **Brain** (`brain/jarvis/`, all compiles) = orchestrator (two-lane barge-in loop), router (Haiku chat vs Claude Code work), action_contract (raw prose never acts; fenced `jarvis` block only), state_server (WS→HUD), voice/{wake,stt,tts} adapters (openWakeWord + faster/mlx-whisper + Kokoro/Polly), memory (auto-redaction), vault_client. **Vault** (`vault/`, FastAPI broker, compiles) = the secrets broker (chat/code/tts/confirm + audit + confirm-gate + Claude-Code-via-`claude -p`-on-Max-sub). **Supervision** (launchd plist + doctor.py) + **scripts** (setup-mac.sh / run-dev.sh / prove-private.sh).

**User's "next-level UI" ask (2026-06-12, folded in):** HUD must show it working live (tests/demos like Claude = `lab` scene), physically see it drive a real browser of the app (`cast` scene, Playwright/CDP screencast — demo uses a real RaisingTenders screenshot), and a UI that ADAPTS to commands (e.g. live Brisbane map) in the Jarvis aesthetic via a widget registry (`canvas` scene) + sandboxed generative views. All = SPEC §6.5.

**Validation:** researched real builds — our design = hardened superset of `ethanplusai/jarvis` (almost identical, already shipped; we add wake word + barge-in it lacks); vault/broker = Tailscale "Aperture" pattern (production-grade, ahead of hobby builds that leak `.env` per device). See docs/RESEARCH_NOTES.md.

**Waits for the Mac:** live voice loop (mic/speakers), vault container running, launchd, Pipecat for production barge-in — all marked `TODO(mac)`; RUNBOOK walks it (~60-90 min). **Still open:** first capability (smart-home / dev-cockpit / briefings), enable Polly or stay local, Pipecat now-or-later.

**HOME MAC ARRIVED (2026-06-13):** it's an **M2 Pro** (better than the planned M1 Pro — no changes, runs snappier). **Built-in screen is BROKEN → used Mac-mini-style with the 2 external monitors + the TCL 4K TV.** Confirmed = always-on home base. **Setup approach = hand off to a fresh Claude running on the Mac** (their "Claude at root on the clean Mac" plan). Key clarification given: that Mac's Claude does NOT auto-sync this container's memory/history — context must travel in the folder. So I bundled it: added **`jarvis/docs/CONTEXT_FOR_MAC.md`** (briefing: task + guardrails + locked decisions + woahh summary) + copied **`jarvis/docs/context/jarvis-project-memory.md`** (this memory) + **`jarvis/docs/context/woahh-CLAUDE.md`** (master woahh context) into the folder, then re-bundled `jarvis-bundle.tar.gz`. Transfer = AirDrop (the dev container bind-mounts the roaming Mac's `/Users`, so the folder is on the roaming Mac's disk). Broken-screen notes: connect an external monitor BEFORE first boot (Apple Silicon outputs to externals at Setup Assistant); run **lid-open** (screen dead) so built-in mic/speakers work; `sudo pmset -c sleep 0 disablesleep 1`. Bootstrap on Mac = Homebrew + node + tailscale + orbstack + `npm i -g @anthropic-ai/claude-code` + `claude` login (Max) + `claude setup-token`, then open `claude` in `~/jarvis` and hand it CONTEXT_FOR_MAC.md + RUNBOOK.md.

**MILESTONE (2026-06-13): JARVIS BUILT + LIVE on the home Mac, now being improved THERE by the Mac's own Claude instance.** Handoff worked. **Source-of-truth shift:** the Mac's `~/jarvis` is now the canonical, evolving version; the dev-container's `/workspaces/GrowthHub/jarvis/` is the original SEED and is now likely STALE/diverging. **Recommended next:** give Jarvis its own dedicated **private git repo** (separate from the woahh app + hub-files repos) so the Mac, the container, and future sessions can sync + so changes are reviewable. **My ongoing role = architect/reviewer:** user pastes what the Mac's Claude did → I check it against the locked design + security model (barge-in/echo AEC, action-contract "raw prose never acts", vault audit+confirm gate, lsof privacy test, Max-credit overflow behavior, supervision). **Still-open decisions as it's improved:** first capability (smart-home / dev-cockpit / briefings), enable Polly "Brian" vs stay local Kokoro, wire Pipecat for production barge-in.

**Offered next:** scaffold the HUD web app + agent skeleton NOW in the container (previewable via the chromium screenshot harness), so it's plug-and-play onto the home Mac.
