# Fixes for woahh app — punch list (2026-06-10)

> **Single source of truth for fixes/polish.** Rewritten 2026-06-10 from the founder's list.
> The old 2026-06-02 punch list is in git history; everything from it that was still open is
> carried over in the "Carried-over backlog" section at the bottom.
> Work the numbered list top-to-bottom unless the founder reorders.

## ⚡ Status — updated 2026-06-11 (for the team)

**Legend:** `[x]` = done (where it lives is noted on the item) · `[ ]` = open. ✅ The overnight-fixes branch was **MERGED to `main` on 2026-06-12** — ticked items below are live on prod (Cloudflare rebuilds from main).

- ✅ Ticked below: **2, 4 (core), 5, 6, 7, 9, 10, 11 (already-compliant), 13, 15, 16, 17** (overnight branch) + **25** (research stage — plan doc delivered).
- ⏸ Holds/decisions for the founder: **1** (ABN gate), **8** (slug domain DNS), **12** (phone UI hold), **14** (charity money-flow — net-new build), **20/21** (decisions).
- 🔜 Open + actionable now: **3** (manual test), **#4 spacing follow-up**, **18, 19 (money bug!), 22, 23, 24, 26, 27**.
- 🔑 Founder side-quests: run `docs/sql/FOUNDER_RUN_THESE.sql` migrations, run `docs/sql/SECURITY_FIXES_RUN_THESE.sql` (audit 2026-06-11), key rotation.

## ⚡ 2026-06-13 — checkout prefill + auth polish SHIPPED to main
Account-prefilled checkout, phone mandatory at signup, single consent tick, auto-sign-in
after code verify (+ fixed a wrong-Supabase-client bug). Edge fn redeployed. Build green,
merged `869d676`. **Founder morning test pending** (real phone — Turnstile blocks headless).
This closes #11 (single tick) + advances #15/#32.

## ✅ BUILT overnight 2026-06-12 (claude → branch `feat/founder-fixes-2026-06-12`, pushed for preview, NOT merged)
> **Stayed off** #4/#26 (Adithya: `feat/sidebar-search-collapse`) + the landing (yieldarche: `new-landing`).
> **Verified ALREADY DONE (no work needed):** #18 free-trial banner, **[6.4]** customer-PII RLS.
> **Built + pushed (preview-testable tomorrow):** #28 chunk-reload · #19 pending-order urgency (flashing card + escalating Web-Audio alarm + mute) · #22 add-to-cart "Added ✓" toast on both storefronts + qty stepper · #23 KDS on-board fulfillment filter tabs w/ counts · #27 staff "View receipt" modal · #24 Order History page (completed log + owner refund ledger + staff-PIN-gated refunds) · #31 customer-queries-email field · #21-NOW storefront-edit lock flag + retail templates filtered.
> **Tested:** all 5 new dashboard pages smoke-pass (Playwright, demo mode — render clean, zero console errors); KDS filter bar confirmed. `tsc` + `vite build` green.
> **⚠️ Needs a founder DEPLOY to fully work on live** (edge fns, not on the Cloudflare preview): #24 staff PIN gate (`staff-pin-login` new `verify_pin` action), #30 one-email defaults (`order-notify`). Owner refunds + the receipt reply-to already work.
> **Security audit of the night's diff: run + results folded in (see below).**

## The list

- [ ] **1. Nicer onboarding checklist — and no payments until ABN verified.** Rework the OnboardingChecklist into a clearer, nicer flow; hard-gate payment acceptance (online card + pay-at-venue config) until the merchant's ABN is verified.
- [x] **2. "Get set up" section auto-clears done steps.** Each setup step disappears (or collapses as ✓ done) once completed, so the section only ever shows what's left. **✅ MERGED to `main` 2026-06-12 — live on prod.**
- [ ] **3. Test the in-store validation code.** End-to-end test of the rotating 6-digit in-person loyalty codes (earn + redeem; customer Account "In-Store" tab ↔ dashboard Loyalty validator).
- [x] **4. Declutter the sidebar.** Grouped/collapsible nav **✅ MERGED to `main` 2026-06-12** — but see open follow-up below.
  - [ ] **OPEN follow-up (founder feedback 2026-06-11 + 12) — IN PROGRESS on `feat/sidebar-search-collapse` (pushed, unmerged; incl. spacing tighten + sidebar search + collapse toggle):** the grouped/dropdown version is **too spaced out** — tighten the vertical rhythm (smaller gaps, denser dropdown padding, more nav per screen). **AND reorganise the group contents** — items landed in the wrong dropdowns (e.g. Analytics ended up under Settings; belongs in Manage — see #26). Re-sort every item by usage frequency: daily-ops up top (Orders, Menu, Analytics, Customers…), configuration at the bottom (Settings, Branding…), and sanity-check each dropdown's members with the founder before merging.
- [x] **5. Obvious "setting up & domain" checklist.** A prominent checklist that walks the merchant through setup including their `<slug>.woahh.app` domain/storefront go-live. **✅ MERGED to `main` 2026-06-12 — live on prod.**
- [x] **6. Disable delivery everywhere, temporarily.** Hide/disable delivery across storefront, checkout, dashboard, KDS, marketing copy (keep courier code dormant behind a feature flag — carries over old TODO 6.1). **✅ MERGED to `main` 2026-06-12 — live on prod.**
- [x] **7. Storefront only viewable live once the merchant publishes.** Unpublished storefronts must not be publicly reachable. **✅ MERGED to `main` 2026-06-12 — live on prod.**
- [ ] **8. On publish, put it on the `<slug>.woahh.app` domain.** Publishing is what activates the merchant's subdomain.
- [x] **9. Limit storefront changes.** Rate-limit / restrict how much the merchant can fiddle with the storefront (template/branding churn) so they can't endlessly play with the web presence. **✅ MERGED to `main` 2026-06-12 — live on prod.**
- [x] **10. Fix checkout timings — show proper live timings.** Pickup/prep time shown at checkout must be real and live, not placeholder. **✅ MERGED to `main` 2026-06-12 — live on prod.**
- [x] **11. One checkbox at checkout for Terms + marketing.** **✅ VERIFIED ALREADY-COMPLIANT 2026-06-11 (no change shipped — current wording passes; see compliance note).** Combine the Terms-accept and marketing opt-in into a single checkbox. ⚠️ Compliance note: Spam Act consent should be express/freely given — bundling marketing into the mandatory Terms tick is risky; recommend one *required* Terms checkbox with clearly-worded marketing consent line, or confirm wording against `docs/legal/legalities.md` §6 before shipping.
- [ ] **12. Fix phone UI for a better experience.** ⏸ ON HOLD until the whole new UI lands — quick wins only if trivial.
- [x] **13. Pay at venue or pay online — merchant-configurable.** Merchant setting that controls which payment options the customer sees at checkout. **✅ MERGED to `main` 2026-06-12 — live on prod.**
- [ ] **14. Fix the order money flow for the contributor/charity section.** Order → commission → charity split must flow correctly into the donation ledger / impact totals (per the locked 3%+1%=4% → 2%/2% model).
- [x] **15. Checkout offers customer account before guest.** Flip the order: sign-in/create-account is the primary path, guest checkout secondary. *(Founder item 16.)* **✅ MERGED to `main` 2026-06-12 — live on prod.**
- [x] **16. Smarter automatic usernames.** Auto-generated usernames get numeric suffixes (e.g. `priya`, `priya2`, `priya17`) instead of failing or producing ugly handles. *(Founder item 17.)* **✅ MERGED to `main` 2026-06-12 — live on prod.**
- [x] **17. QR codes accessible without enabling dine-in.** A merchant should be able to generate/use QR codes even when dine-in is off. *(Founder item 18.)* **✅ MERGED to `main` 2026-06-12 — live on prod.**
- [x] **18. Fix the free-trial banner on accounts. ✅ VERIFIED ALREADY IMPLEMENTED 2026-06-12** — `DashboardLayout` renders the banner only when `tier === 'free_trial'` and computes live days via `trialDaysLeft()` (`lib/tier.ts`: `ceil((trial_ends_at - now)/day)`); hidden for non-trial orgs + staff. No static "30 days". *(No code change needed.)* *(Added 2026-06-11.)*
- [x] **19. Auto-decline + pending-order urgency — ✅ DONE 2026-06-12 (`feat/founder-fixes`).** Backend auto-decline auth fixed earlier this session (`order-respond` v40 accepts service_role JWT). UX shipped: awaiting cards pulse a red ring in the final minute (amber border in the warning window) + an escalating Web-Audio alarm (18s→8s→4s) with an owner mute toggle. Original: Orders in `awaiting_confirmation` count up forever instead of auto-declining at `settings.orders.confirmation_timeout_minutes` (default 7). Likely suspects: the `auto_decline_stale_orders()` pg_cron job not actually scheduled/enabled on live (`pmnyhbhtkcfoozkinieo`), or its call into `order-respond` failing auth — verify the cron exists, check its run history, and test an order through the full timeout. ⚠️ With online cards this is a money bug: manual-capture auths rely on the auto-decline to void — an order that never declines can leave a customer's hold dangling. **Plus UX:** escalate urgency on pending orders as the clock runs — flashing highlight on the order card as it ages toward timeout, and/or a repeating notification sound until accepted. *(Added 2026-06-11.)*
- [x] **20. Customer-account model — ✅ DECIDED 2026-06-12: unified Woahh account.** Full locked spec incl. consent wording, cross-shop privacy rules, guest nudge placement: `docs/architecture/UNIFIED_CUSTOMER_AUTH.md`. Build started on `feat/unified-customer-auth`. Original framing: Map how the order-time account flow actually works today (guest anon-session checkout, magic-link sign-in, username lookup, `growthhub_profiles` + `merchant_connections` unified identity) and pick the model for now: (a) **one universal Woahh customer account** used across every merchant (the `growthhub_profiles` infra already exists and merges by email+phone — cross-merchant loyalty/orders hub), (b) per-merchant accounts (feels owned by the merchant, but fragments identity + re-consent each shop), or (c) keep guest-first with an optional account nudge. Decision interacts with #15 (account-before-guest ordering) and the Spam Act consent scoping (consent is per-merchant either way). Recommendation to evaluate: universal Woahh account, branded lightly per-merchant at checkout. *(Added 2026-06-11.)*

- [ ] **21. Storefront branding & template picker v2 — lock now, build after 5 merchants. 🔶 PHASED.**
  - **NOW — ✅ DONE 2026-06-12 (`feat/founder-fixes`):** per-org `settings.storefront.self_edit_locked` flag (default off) renders a read-only "Woahh is setting up your storefront" notice on Branding + Storefront-template pages — flip it per merchant during onboarding (`StorefrontLockNotice.tsx` has the SQL). Retail-only templates filtered out of the picker. *(Build the "AFTER 5 merchants" items below later.)* temporarily **lock the Storefront + Branding pages for merchants** — read-only or hidden; Woahh sets each merchant's template + website design by hand for the first onboardings. (Supersedes/extends #9's rate-limit — a full lock is simpler for this phase.) Also **remove the retail color presets** from Branding (retail vertical is deferred).
  - **AFTER onboarding 5 merchants (build then):**
    - **Logo-driven branding:** merchant's uploaded logo is used in storefront branding, and brand colors are **auto-extracted from the logo** (palette suggestion they can accept/tweak — must still pass the HSL-allowlist guard).
    - **Design-first template picker:** previews show the actual **template designs/layouts** to pick from, not just color variations — templates are design, colors are a separate knob.
    - **Pre-publish preview ("ghost link"):** before publishing, merchant sees exactly how the site looks with their picked template + colors — a tokenized/unlisted preview URL they can open on their phone or share, without the storefront going live (ties into #7 publish-gating and #8 subdomain-on-publish).
    - **Customer login/sign-up in the templates:** every storefront template gets a visible customer sign-in / create-account entry point (header + checkout). 🔶 Mechanism TBD with founder — via the universal Woahh account or something merchant-scoped; depends on the #20 decision, build them together.
  *(Added 2026-06-11.)*

- [x] **22. ✅ DONE 2026-06-12 (`feat/founder-fixes`)** — every add fires an "Added to cart ✓" toast on BOTH the default + bespoke storefronts (the bespoke path used to close silently); quantity stepper added to the customize dialog; fly-to-cart + badge pulse already existed. **Obvious add-to-cart feedback + add dialog.** Right now pressing "Add" gives no clear signal the item landed in the cart. Two parts: **(a)** on every Add, show a popup/sheet asking **quantity + modifications** (extras, remove-ingredients — the existing customize dialog already does this for items with options; make it consistent for all items, with a fast default path so simple items stay one-tap-ish); **(b)** unmistakable added-feedback — cart icon badge count bumps with a pop animation / item "flies" to the cart / brief "Added ✓" state on the button. Mobile especially (cart is off-screen there). *(Added 2026-06-11.)*

- [x] **23. ✅ DONE 2026-06-12 (`feat/founder-fixes`)** — on-board fulfillment filter tabs (All / Dine-in / Pickup / Delivery) with live per-type counts + big tap targets, right on the KDS (was settings-only). Big-text PICKUP/TABLE labels already existed. *(Lane grouping within a column deferred — filter tabs deliver the "work all pickups" need.)* **Kitchen order screen — more convenient for staff.** Pickup vs dine-in must be tellable at a glance from across the kitchen. There IS existing color coding (fulfillment header bar: dine-in blue, pickup purple, etc.) but it's evidently not landing — go stronger: big text label/icon ("PICKUP" / "TABLE 12") not just color (color-blind + greasy-screen friendly), prominent table number / pickup name, and **filter tabs or grouped lanes by fulfillment type** so staff can work "all dine-in" or "all pickups" in one view. While in there: general staff-convenience pass with real kitchen feedback (font sizes at arm's length, tap targets, what info actually matters per ticket). *(Added 2026-06-11.)*

- [x] **24. ✅ DONE 2026-06-12 (`feat/founder-fixes`)** — new Order History page (`/business/dashboard/order-history`, linked from the Orders header): completed-orders log (search + 7/30/90d, payment, totals, View receipt) + owner-only Refunds ledger (amount, reason, who authorised, status, running total) + non-owner staff refunds gated by 6-digit PIN re-entry (`staff-pin-login` new `verify_pin` action — ⚠️ needs deploy). The `payment_refunds`/`security_audit_log` data layer already existed. **Better completed-order log + staff-coded, owner-visible refunds.** Three parts: **(a)** a proper completed-orders log view (clean history: date/time, items, fulfillment, payment method, totals, search/filter — not just the kanban's tail); **(b)** refunds on a **staff** session require the staff member's **PIN/staff code re-entry to confirm** (re-auth at the moment of refund — the PIN infra already exists from staff login), and every refund is **logged with who authorised it** (staff name, amount, reason, order ref, timestamp); **(c)** an **owner-only dashboard section listing all refunds** (per staff member, per period) so the owner sees refund activity at a glance. Wire into the existing refund mechanics (`REFUND_POLICY.md` / `payment_refunds`). *(Added 2026-06-11.)*

- [x] **25. Square Register integration — RESEARCH ✅ DONE 2026-06-11** (plan delivered: `docs/architecture/SQUARE_REGISTER_INTEGRATION.md` — Orders-API push is the play; build phases 0–4 are NEW work, not started). First merchant runs the big Square iPad-lookalike device (Square Register or iPad + Square Stand). Figure out how Woahh links to it: can we push Woahh online orders onto their Square device (Orders API push → Register order screen/KDS), can we drive in-person payments on it (Terminal API device support — Terminal vs Register, AU availability), how do Register-side actions sync back (webhooks → order status/GMV reconciliation in Woahh), and what partner/OAuth scopes are needed. Builds on the existing Square connector (`docs/architecture/SQUARE_POS_INTEGRATION.md` — OAuth, CreatePayment, refunds, locations already sandbox-built). Deliverable: `docs/architecture/SQUARE_REGISTER_INTEGRATION.md` with ranked integration options + recommendation. *(Added 2026-06-11; multi-agent research run kicked off same day.)*

- [ ] **26. Analytics page — found (misfiled), move it up into Manage.** Update 2026-06-12: it wasn't deleted — the sidebar regroup tucked Analytics under **Settings**, which is why it "disappeared". Move it into the **Manage** group near the top (it's a daily-use page, not configuration), as part of the #4 sidebar reorganisation below. Keep the regression check: every dashboard page reachable from the nav, in a group that matches how often it's used.
- [x] **27. ✅ DONE 2026-06-12 (`feat/founder-fixes`)** — "View receipt" on the Orders page (and OrderStatus) opens an on-screen receipt (`ReceiptView`) built from the SAME `buildReceiptModel()` the email/print/PDF use, so it can't drift. Also reachable from the #24 history log. *(KDS receipt button = small follow-up.)* **Staff can view the full receipt — identical to the customer's emailed one.** From an order (Orders page, KDS, and the completed-order log in #24), staff/owner can open the exact receipt the customer was emailed (same AU tax-invoice content: items, add-ons/mods, totals, payment, pickup#/table#) — to cross-check "that's not what I ordered" disputes with proof of what was actually confirmed. Render from the same receipt template/data as the email so they can never drift apart. *(Added 2026-06-11.)*

- [x] **28. ✅ DONE 2026-06-12 (`feat/founder-fixes`)** — `vite:preloadError` + `unhandledrejection` listeners in `main.tsx` auto-reload once (sessionStorage loop-guard); ErrorBoundary detects ChunkLoadError → quiet "Updating…" + reload instead of "Something went wrong". `src/lib/chunkReload.ts`. **"Reload/refresh" error when opening pages (Promotions et al.) — ROOT CAUSE FOUND, fix pending.** What's happening: the dashboard code-splits every page (46 `lazy()` routes in `App.tsx`). Each deploy to `main` gives chunk files new hashed names — any browser tab opened BEFORE the deploy still runs the old router, and the first page it tries to lazy-load after the deploy 404s (old chunk gone) → the ErrorBoundary shows "Something went wrong — Refresh the page". After refresh the new HTML+chunks load, so it works. It'll hit whichever page is clicked first after any deploy — Promotions today, anything tomorrow; we deploy often, so merchants see it a lot. **Fix (small, ~20 lines):** (a) listen for Vite's built-in `vite:preloadError` event in `main.tsx` → `preventDefault()` + auto-reload once (sessionStorage guard against reload loops); (b) make the ErrorBoundary recognise chunk-load failures and silently reload once instead of showing the scary screen; (c) optional polish: a "new version available" toast via build-id check instead of hard reload mid-task. No code currently handles this (verified: zero `vite:preloadError`/ChunkLoadError handling in `src/`). *(Added 2026-06-12, diagnosed same day.)*

- [x] **29. Apple Pay + Google Pay — RESEARCH ✅ DONE 2026-06-12** (plan delivered: `docs/architecture/APPLE_GOOGLE_PAY_PLAN.md`; verdict: fully doable in AU, zero extra fees, capture-on-confirm survives intact; build = Stripe Express Checkout Element first ~3-5 days, then subdomain register-on-publish hook, then Square parity. IMPLEMENTATION not started). Customers pay orders with Apple Pay / Google Pay on the web checkout, for BOTH providers (Stripe Express Checkout path + Square Web Payments SDK path), preserving the capture-on-confirm flow (authorize at wallet-tap, capture when the restaurant accepts). Key question being verified: Apple Pay's per-domain verification × our per-merchant `<slug>.woahh.app` subdomains (needs automated domain registration on publish). Deliverable: `docs/architecture/APPLE_GOOGLE_PAY_PLAN.md` with how-it-works + implementation phases. *(Added 2026-06-12.)*

- [x] **30. ✅ DONE 2026-06-12 (`feat/founder-fixes`) — ⚠️ needs `order-notify` deploy.** Per-status preparing/ready emails flipped from on-by-default to opt-in (`order-notify`: `=== true`; NotificationSettings DEFAULTS match) → the customer gets ONE email (the confirmation receipt from `order-respond`) + the tracker; decline outcome email unchanged. **Stop per-order email spam — exactly ONE email, on confirmation.** Audit every email the order lifecycle currently fires at the customer (placed/confirmed/preparing/ready/completed via `order-notify` triggers + the receipt). New rule: the customer gets a **single email when the order is CONFIRMED**, containing the full receipt (the existing combined AU tax-invoice email) + the live order-tracker link — everything else happens on the tracker page (status changes) or via web-push if they opted in. Kill/disable the other per-status emails by default; make the NotificationSettings triggers default to confirmed-only so merchants don't accidentally re-spam. *(Added 2026-06-12.)*

- [x] **31. ✅ DONE 2026-06-12 (`feat/founder-fixes`)** — added an editable "Customer queries email" field in Operations → Business Details (writes `contact_email`) with a "replies come to you, not Woahh" note. The transactional senders (`order-respond`, `order-notify`, `send-transactional-email`) already set Reply-To from `contact_email`, so this just exposes the control. *(Optional: a "just reply" footer line in the email body — small follow-up.)* **Merchant reply-to address on customer emails.** Customers can't reply to our order/transactional emails today (sender is the platform's mail domain). Let each merchant set a "customer queries" email address in settings, and set **Reply-To** on all customer-facing emails (order confirmation/receipt, decline, reservation confirms/reminders) to that address — so hitting reply goes straight to the merchant, not to us. Note: the marketing/campaign sender already does Reply-To → `organizations.contact_email`; this extends the same pattern to transactional sends, with a dedicated editable field (default = contact_email) + clear "questions about your order? just reply" line in the email footer. *(Added 2026-06-12.)*
- [x] **32. Unified customer auth — ✅ BUILT + MERGED to `main` + LIVE 2026-06-12.** Migrations applied, edge fns deployed, full flow live-tested (signup→code→session, rate-limit/lockout, replay-block), 2 security risks fixed (Turnstile gating + unconfirmed-account purge), **Turnstile ENABLED** (site key + secret set by founder, enforcement confirmed live). In-dialog signup w/ 6-digit email code (zero redirects, cart survives), 3 sign-in methods (password/email code/SMS code w/ anti-pumping armor), merchant-logo trust header, generic cross-shop notice, one-tick consent (locked wording), loyalty back-claim, receipt PS guest nudge. ⚠️ MERGE ORDER: founder runs migration `20260612200000` + deploys `customer-auth-otp` & `order-respond` edge fns FIRST, then merge. Staged next: opt-in follow-up email cron, members-only promo UI. Spec: `docs/architecture/UNIFIED_CUSTOMER_AUTH.md`. LOCKED: password + email code + SMS code at launch (SMS armored: rate limits, lockouts, Turnstile-ready); in-dialog 6-digit email verification, no redirects; one-tick consent w/ explicit marketing wording; receipt-PS guest nudge + opt-in-only follow-up; members_only promo flag. Original framing: The current customer flow (email magic link + username lookup) is slow at checkout. Add quicker options: **sign in with mobile number + SMS code** and/or a **password** option at account creation — or similar low-friction modern equivalent (passkeys worth evaluating: zero typing after first setup, no SMS cost). Considerations: per-SMS cost via ClickSend (OTP sends should ride the shared platform number), Spam-Act-clean (OTP = transactional), and it must slot into the #20 account-model decision + #15 account-before-guest checkout order — design these three together. *(Added 2026-06-12.)*

- [x] **33. Sign-in behind checkout on phones — ✅ FIXED + MERGED to `main` 2026-06-12 (live on prod).** Root cause: shadcn dialogs defaulted z-50 under the z-200 checkout panel (same class as the old card-dialog bug, which was only patched ad hoc). Shipped a global tokenized layering scale (`docs/OVERLAY_LAYERING.md` in the app repo): chrome 150 < screens 200 < modals 300 < floats 320 — so sign-in/card/any future dialog clears every full-screen panel on every template, current and future. Enforcement: `check:layers` guard now runs on EVERY Cloudflare deploy (prebuild hook) and bans raw z values in all customer-facing code. Bonus mobile hardening from the 3-lens specialist review: dialogs rounded+inset+keyboard-safe on phones, 44px close target, magic-link dead-end fixed (+resend), inline sign-in errors, email autofocus, "Create account" no longer loses the cart (new tab), checkout/product takeovers lock background scroll. Verified: 390px Playwright probe — auth dialog z=300 atop checkout z=200, zero console errors. Bug: opening sign-in from checkout renders the auth screen/dialog BEHIND the checkout sheet on mobile (stacking/z-index/portal-order issue — likely two overlays mounted in conflicting portals), so the phone user sees a dead checkout. Fix the layering (one overlay manager: sign-in must mount above, or checkout sheet must yield), and test the full sign-in-at-checkout path at 390px. **Bigger point (founder, 2026-06-12 — this class of bug has happened before):** make phone compatibility a standing pre-merge gate, not a hope — every checkout/auth/storefront change gets a 390px viewport pass (we already have the Playwright harness for fixed-viewport shots + flows) before it merges; overlay/dialog/sheet components especially. Relates to #12 (full phone-UI revamp on hold) but is NOT held — broken-on-phone bugs always fix now. *(Added 2026-06-12.)*

> Original founder list skipped #15, so items 16–18 are renumbered 15–17 here.

**When this list is done → check back with the founder before starting anything else.**

## Carried-over backlog (still open from the old list, verified 2026-06-10)

Not part of the active list above — pick up after it, or when a hard gate forces one.

- **[6.2] Founding launch promo** — free subscription (1 yr / lifetime OPEN) + temporary zero commission for first N sign-ups; reconcile wording with existing founding terms; backend enforcement + sign-up code gating (old `-1.2`) still missing.
- **[6.4] Restrict customer PII to owner + manager — ✅ VERIFIED DONE 2026-06-12.** Migration `20260611020000` already DROPPED the `"Staff view customers"` RLS policy; the customers SELECT policy no longer covers lower staff roles. *(No code change needed.)*
- **[6.6] Storefront platform — finish + ship** — wildcard `*.woahh.app` DNS/TLS + Pages custom domain (human step), template-picker dashboard page, per-merchant PWA icons. Items 5/7/8/9 above bite into this.
- **[3.1] Hard separation of merchant vs customer auth identities** — routing split is done; DB-level separation pending.
- **[4.2] In-person checkout: attach/invite customer for loyalty** at point of service.
- **[4.3] Receipts: print + PDF + in-person** — email receipt for online orders is DONE; the rest pending (KDS/docket print button follow-up lives here too).
- **[4.4] Installable PWA for the merchant dashboard/KDS** — install prompt + offline shell.
- **[5.1] Storefront mobile checkout polish** — floating cart bar + slide-up sheet (overlaps item 12's hold).
- **[6.3] UI uplift — residual polish** (most shipped 2026-06-02 with the green/gold theme; rebrand-preview decision pending).
- **[6.5] Franchise / multi-location layer** — designed (`docs/architecture/FRANCHISE_ARCHITECTURE.md`), build post-onboarding.
- **[sec] `cost_price_cents` out of staff-readable `products`** — gate before enabling retail.
- **[sec] Reviews RLS edge-case audit** — SELECT/UPDATE policy consistency.
- **[sms] Per-merchant SMS productionise** — buy per-merchant ClickSend numbers + shared OTP number; assign via AdminSmsNumbers; consider Cellcast at volume.
- **[pos] POS & in-person payments** — Stripe Terminal Phase 1 / Square Terminal; long-lead blockers (Tap to Pay entitlement, AFSL confirmation, AU Square account + PAAF PDS/FSG).
- **[ops] Rotate exposed keys** — ClickSend, GitHub PATs, Supabase `sbp_`/`sb_secret_`, Anthropic, Resend `re_`/`whsec_` (full list in `docs/architecture/SMS_ARCHITECTURE.md` "Remaining").
